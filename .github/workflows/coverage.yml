# Coverage workflow — phenotype-infrakit archived; inline stub until tarpaulin gate restored
name: Coverage

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  coverage:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v4
      - name: Coverage placeholder
        run: echo "Coverage gate deferred — infrakit drain complete; see Test workflow for unit coverage."
