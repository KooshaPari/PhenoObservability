# Generated CI workflow - calls reusable phenotype workflow
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  ci:
    uses: KooshaPari/phenoShared/.github/workflows/ci.yml@c3aee3361ff4888a8ced71c8861a8d54ae65fca0
    secrets: inherit
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
